var expect = require('expect');

// BEFORE the describe() function, this was just here as-is:
// it('should properly run tests!', () => {
//   expect(1).toBe(1);
// });

describe('App', () => {
  it('should properly run tests!', () => {
    expect(1).toBe(1);
  });
});
